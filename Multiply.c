#include <stdio.h>
int main()
{
    long long a;
    long long b;
    scanf("%lld",&a);
    scanf("%lld",&b);
    long long multiflication = a*b;
    printf("%lld",multiflication);
    return 0;
}